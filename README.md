# ome-zarr-py-c

This repository vendors the upstream `ome/ome-zarr-py` `v0.15.0` release
snapshot under `source_code_v.0.15.0/`.

That folder is frozen. Do not edit files inside it directly.

Future work should live outside `source_code_v.0.15.0/`.
